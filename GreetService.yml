spring:
  application:
    name: GreetService
server:
  port: 9092

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9090/eureka



management:
  endpoints:
    web:
      exposure:
        include: "*"